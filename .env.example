# AutoUpdater Environment Configuration

# Required: SSH User for remote deployments
SSH_USER=deploy

# Optional: SSH Password (use SSH keys instead for production)
SSH_PASSWORD=your_password

# Optional: Host address for container communication
HOST_ADDRESS=172.17.0.1

# Optional: Override default ports
HOST_PORT=8080